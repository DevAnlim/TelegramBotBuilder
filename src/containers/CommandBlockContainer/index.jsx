import CommandBlock from '../../components/CommandBlock';

export default function CommandBlockContainer() {
  const handleClick = () => {};

  return <CommandBlock onClick={handleClick}></CommandBlock>;
}
